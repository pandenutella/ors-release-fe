const initialAccounts = [{ username: "bacaljo", password: "123" }];

export default function accounts(state = initialAccounts, action) {
    switch (action.type) {
        default:
            return state;
    }
}
